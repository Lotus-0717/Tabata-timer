module.exports = {
  singleQuote: true, // 使用單引號
  semi: false, // 結尾不使用分號
  printWidth: 120, // 每行代碼長度（預設值是80）
  trailingComma: 'es5', // 對象或數組最後一個元素後面是否加逗號（在ES5中有效的地方加上）
  tabWidth: 4, // 每個tab相當於多少個空格（預設值是2）
  useTabs: false, // 是否使用tab進行縮進（預設值是false）
  bracketSpacing: true, // 對象字面量的大括號之間是否有空格（預設值是true）
  arrowParens: 'avoid', // 箭頭函數單個參數是否加括號（avoid - 避免）
};
